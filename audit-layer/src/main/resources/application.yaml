server:
  port: 9999

spring:
  datasource:
    url: mongodb://localhost:27017/ticket-reg-info
    username: test
    password: test
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  activemq:
    user: admin
    password: admin
    broker-url: tcp://127.0.0.1:8161
    packages:
      trust-all: true
    pool:
      enabled: true
      max-connections: 5


